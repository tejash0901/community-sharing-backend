package com.community.toolsharing.backend.model;

public enum BookingStatus {
    PENDING,
    APPROVED,
    COLLECT_PENDING,
    COLLECTED,
    RETURN_PENDING,
    RETURN_REJECTED,
    RETURNED,
    REJECTED,
    CANCELLED
}
